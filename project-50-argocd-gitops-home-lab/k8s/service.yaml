apiVersion: v1
kind: Service
metadata:
  name: hello-gitops
  namespace: gitops-demo
spec:
  type: ClusterIP
  selector:
    app: hello-gitops
  ports:
    - name: http
      port: 80
      targetPort: 80

